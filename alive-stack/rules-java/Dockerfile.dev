# Development Dockerfile with Spring Boot DevTools for hot reload
FROM maven:3.9-eclipse-temurin-17

WORKDIR /app

# Copy pom.xml first for dependency caching
COPY pom.xml .

# Download dependencies (cached layer)
RUN mvn dependency:go-offline -B

# Source will be mounted via volume
EXPOSE 8080

# Use spring-boot:run with DevTools for hot reload
# DevTools watches for class changes and restarts automatically
CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.jvmArguments=-Dspring.devtools.restart.enabled=true"]
